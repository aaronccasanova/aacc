{
  "name": "aacc",
  "private": true,
  "scripts": {
    "initialize": "npm i && npm run clean && lerna bootstrap",
    "clean": "lerna clean --yes && lerna exec -- rm -rf dist",
    "refresh": "lerna bootstrap",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.6.2"
  }
}
