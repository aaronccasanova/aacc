{
  "name": "aacc",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "test": "turbo run test",
    "build": "turbo run build",
    "clean": "rm -rf node_modules && rm -rf packages/*/{node_modules,dist,.turbo}"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "turbo": "^1.2.6",
    "typescript": "^4.6.3"
  }
}
