import { createDesignTokens } from 'designtokens.io'

import {
  breakpoints,
  colors,
  shape,
  spacing,
  typography,
} from '../token-groups'

export const commonTheme = createDesignTokens({
  breakpoints,
  colors,
  shape,
  spacing,
  typography,
})
